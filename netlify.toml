[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  # npm run check runs node --experimental-strip-types
  NODE_VERSION = "22"

[dev]
  framework = "vite"
  targetPort = 5173
